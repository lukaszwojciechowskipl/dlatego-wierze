// Phase 0 placeholder — schemas added in Phase 1.
// Each schema definition will live in src/schemas/documents or src/schemas/objects
// and be re-exported from this barrel as a flat array of schema types.

import type { SchemaTypeDefinition } from 'sanity';

export const schemaTypes: SchemaTypeDefinition[] = [];
